services:
  - type: web
    name: kj-industries-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
    routes:
      - path: /api
        pathPrefix: /api
